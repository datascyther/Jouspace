import React from 'react';

const LottieView = React.forwardRef(function LottieView(_props: any, _ref: any) {
  return null;
});

export default LottieView;
export type AnimationObject = Record<string, unknown>;
export type LottieViewProps = Record<string, unknown>;
