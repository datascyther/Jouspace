{
  "name": "jouspace-runtime-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker build of the Jouspace Intelligence Runtime (free, no card).",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.1",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
